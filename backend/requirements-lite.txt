# Lightweight requirements for Free-Tier deployments
# Keeps core features (API, Auth, DB, Redis, basic Blockchain, Chat) without heavy ML libs

# Web Framework
fastapi==0.109.1
uvicorn[standard]==0.25.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Blockchain (core)
web3==6.15.1
eth-abi==5.0.1
eth-utils==3.0.0
eth-typing==3.5.2
base58==2.1.1

# Databases
neo4j==5.22.0
psycopg2-binary==2.9.9
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1
redis==5.0.1
qdrant-client==1.9.0

# Kafka (client libs kept small; streaming remains disabled by default)
confluent-kafka==2.5.0
avro==1.11.3

# AI & Chat (lightweight)
langchain==0.1.14
langchain-openai==0.0.2
openai==1.7.2
tiktoken==0.5.2

# Security & Auth
python-jose[cryptography]==3.4.0
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.18
email-validator==2.1.0.post1

# HTTP & Utilities
requests==2.32.4
python-dotenv==1.0.0
tenacity==8.2.3
cachetools==5.3.2
prometheus-client==0.19.0
sentry-sdk==2.42.0
psutil==5.9.8

# Optional reporting minimal (omit heavy PDF/Excel toolchain in lite)
# reportlab, openpyxl, pdfplumber, PyPDF2 are omitted here

# NOTE:
# - Heavy ML stacks (torch, torch-geometric, xgboost, scikit-learn, numpy, pandas, scipy) are intentionally excluded.
# - Features depending on these are disabled by default or degrade gracefully.
# - ENABLE_KAFKA_STREAMING remains OFF by default; libs are present to avoid import errors.
